import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.greenandgleamwebsite',
  appName: 'Green and Gleam website',
  webDir: 'dist'
};

export default config;
